import React, { PureComponent } from 'react';
import DeApp from './_deApp';

class Home extends PureComponent {
    render() {
        return (
            <div className="home">
                <DeApp />
            </div>
        );
    }
}

export default Home;
